// import { token } from './lib/utils.ts'

import { diverging } from "./lib/palettes.ts";

// console.log(token({ l: 5, c: 45, h: 87, mode: 'lch' }, { kind: 'arr', omitAlpha: false }))


console.log(diverging(['Spectral', 'RdYlGn']))