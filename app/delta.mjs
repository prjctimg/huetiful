import { average, round, interpolatorPiecewise, lerp, easingInOutSine } from './culori.mjs'
import { interpolatedColors } from './interpolatedColors';





interpolatedColors()
/* 

const piecewiseEasing = (easingFn) => {
    return interpolatorPiecewise((a, b, t) => lerp(a, b)(easingFn(t)))
}

interpolate(['red', 'blue'], 'rgb', { r: piecewiseEasing() })
 */