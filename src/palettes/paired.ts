// @ts-nocheck
// From colorbrewr

import { Color } from "../paramTypes";

/**
 * @function
 * @description Creates a scheme that consists of a series of lightness pair in the passed in color (the lighter variant and darker variant) from the passed in color.
 * @param color The color to return a paired color scheme from.
 */
const pairedScheme = (color: Color): Color[] => {};
