// Group colors by hue with the family as key and the array of colors as values.
import type { Color, factor } from "../paramTypes.ts";

const groupByHue = (colors: Color[]) => {
  const factor: factor = "hue";
  const cb;

  // How do I determine the hue family in which the color is found ?
};
